import { writable } from 'svelte/store';

export const dftilesURL = writable(null);
export const map1URL = writable(null);
export const map2URL = writable(null);
export const map3URL = writable(null);
export const map4URL = writable(null);
